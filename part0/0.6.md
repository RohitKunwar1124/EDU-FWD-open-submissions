## 0.6: New note in SPA – sequence of events

```mermaid
sequenceDiagram
    participant browser
    participant server

    Note over browser: User types note and submits the SPA form
    Note right of browser: JS intercepts submit (preventDefault), updates state and UI immediately
    browser-->>browser: Render updated notes list (optimistic update)

    Note right of browser: SPA sends the new note to server asynchronously
    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa (JSON payload)
    activate server
    server-->>browser: 201 Created / 200 OK (acknowledgement JSON)
    deactivate server

    Note over browser: No page reload; UI already shows the new note
```


